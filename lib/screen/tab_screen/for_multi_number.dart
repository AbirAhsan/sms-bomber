import 'package:flutter/material.dart';

class ForMultiNumber extends StatelessWidget {
  const ForMultiNumber({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Not Implemented Yet",
        style: TextStyle(
          fontSize: 28,
          fontStyle: FontStyle.italic,
          fontWeight: FontWeight.bold,
        ),
      ),
    );
  }
}
