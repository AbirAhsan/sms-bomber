import 'package:flutter/material.dart';

class ForSingleNumber extends StatelessWidget {
  const ForSingleNumber({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
